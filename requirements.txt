# requirements.txt
fastapi==0.133.1
uvicorn==0.41.0
# langchain-core bumped 1.2.14 -> 1.4.3: langchain-google-vertexai>=3.2.4 requires core>=1.3.2.
langchain-core==1.4.3
langchain-google-genai==4.2.1
# Vertex AI provider (USE_VERTEX path). Pulls google-cloud-aiplatform transitively;
# pinned here for reproducible prod builds (matches the locally-verified set).
langchain-google-vertexai==3.2.4
google-cloud-aiplatform==1.157.0
langgraph==1.0.10          # PYSEC-2026-83 (was 1.0.9)
langgraph-checkpoint==4.1.1  # CVE-2026-48775 (was 4.0.0)
supabase==2.28.0
# Direct dependency since the /auth/signup GoTrue forward (was transitive via
# supabase); pinned to the locally-verified version.
httpx==0.28.1
# 1.75.0 is the only window satisfying BOTH langchain-google-genai 4.2.1 (<2.0.0)
# and google-cloud-aiplatform 1.157.0 (>=1.66.0). Supports genai.Client(vertexai=True)
# .embed_content for the Vertex embedding path.
google-genai==1.75.0
python-dotenv==1.2.1
python-multipart==0.0.22
pydantic==2.12.5
aiohttp==3.13.3
langfuse
# Pin to the langgraph-1.0.x-compatible line. Leaving this unpinned makes the
# build non-reproducible: latest langchain (1.3.x) needs langgraph>=1.2.4, so a
# fresh Railway install has to backtrack from the newest release down to 1.2.10,
# which is slow and can fail as new 1.3.x versions land. 1.2.10 requires
# langgraph<1.1.0,>=1.0.8 (matches our pinned 1.0.10) and is what's running in
# prod. The CVE-2026-55443 fix (1.3.9) needs a coordinated langgraph 1.2.x jump —
# tracked as needs-human in docs/security-audit-2026-07.md (F5).
langchain==1.2.10
PyJWT[crypto]==2.13.0  # PYSEC-2026-120/175/176/177/178/179 (auth verify path; was 2.11.0)
cohere>=5.0.0
dateparser==1.2.0
upstash-redis
sentry-sdk[fastapi]
# Pinned to the >=6 keyword-only capture() line that analytics.py targets
# (capture(event, distinct_id=..., properties=...)). Unpinned, a build could
# resurrect the v5 positional signature and silently drop every event.
posthog==7.14.0
razorpay==2.0.1
