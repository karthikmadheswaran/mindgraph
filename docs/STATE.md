# STATE — updated 2026-06-29 (Supabase DB resumed + reachable; local main fast-forwarded to 551938f = prod; 017 `deleted_at` + live deadline read confirmed, index partial-predicate pending a SQL-editor check)

Maintained per ADR-0001: fixed/done items are **deleted** (history lives in the changelog DB + git), never struck through. Keep ≤1.5K tokens.

## Now (≤3)
1. **Vertex AI prod migration** (37fab70 + e2f8a4a; IAM 403 on `mindgraph-vertex` SA fixed 10/06, `roles/aiplatform.user`). Watch prod stability. — Metering review (29/06): the `fix/meter-conversation-routes` work is **already MERGED (PR #5) + LIVE in prod (`551938f`)**, not an open PR. Post-merge audit = GREEN: both 05/06 Criticals closed (paid→pro alias in `tier_service`; in-handler ask/journal metering with ask `record_cost` downstream in `conversation.send_ask_message`), `METER_CONVERSATION_ROUTES` default ON + clean OFF bypass, 5/5 hermetic tests pass. No blockers; nothing to merge.
2. **3.1-flash-lite (B) MERGED 15/06 (PR #2) — only the prod env flip (Next-1) remains; flip vars confirmed in `app/llm.py` 29/06.** B (3.1-flash-lite, thinking_level=minimal, generation node only): reask transforms 13/15, judge mean 4.47 (0.03 under the 4.5 bar), overlap 0.286 vs A 0.487; C (low) fails (3.2). **External concordance audit** (gemini-3.1-pro-preview, 12 blind stratified cases, new seed) = **12/12 pass/fail agreement → external concordance confirmed (12/12)**. **clarifier_commit pre-check 15/15 PASS.** **Full 33-case run (Vertex) vs re-judged 85adc57 baseline, SAME fixed rubric: 32/33 vs 24/33 (+8); reask_loop 0→4/5, clarifier_commit 2→5/5, zero regressions, negatives hold → clears the ±4 noise band.** (Re-judging both neutralized the original Pro confounds: baseline reask 3/5 was trailing-Q luck → 0/5; want_all 2/5 was judge-noise → 5/5.) **Merged to main 15/06** (PR #2, merge `5f5d6da`; `cfaca4c`+`422df32` now on main). **Remaining: Railway env flip → watch prod** (Next-1). **Cost:** generation node $0.0008→$0.0018/conv measured (~2.25×); Batch API (−50%) for eval/offline; context caching ($0.025/M) a post-launch offset for the static prompt prefix. Results: `fullgrid_compare_B_vs_85adc57_*`, `concordance_audit_e7ef0b8e6ac3_*`, `clarifier_precheck_judged_*`, `blindjudge_reask_ABC_*`.

## Drift detection — HERO feature, launch June 30 — branch `feat/drift-intentions` (DO NOT MERGE yet)
Bottom-up: extract stated intentions from prose → persist → surface drift over time. Plan approved; building across the 11-day runway. Critical path: schema → extraction → resolution → backfill → surface.
- **P0 done** (`61b05c6`): `intentions` table — soft-delete + partial unique `idx_intentions_user_entry_text` (UNIQUE on `(user_id, source_entry_id, lower(text)) WHERE deleted_at IS NULL`) + `idx_intentions_live` (`(user_id, status) WHERE deleted_at IS NULL`), mirrors 017; applied + re-verified on Supabase 29/06 via SQL editor; `JournalState.intentions` field. `entries.id` confirmed UUID (014 FK). **`migrations/018_add_intentions.sql` reconstructed from the live schema + committed (`6ff4335`, 29/06)** — table + the two partial indexes, `IF NOT EXISTS` throughout (no-op against live; reproducible on a fresh DB).
- **P1 done** (`9ebc3ef`/`4ea06e3`/`f485050`): `extract_intentions` node — 5th parallel fan-out, flash-lite, prompt + deterministic backstop `drop_non_intentions` (fails-toward-DROP — precision is the gate). Tightened (Option B) after the P3 dry-run exposed two real-data categories the curated fixtures missed: **inverted-urge guard** (drop urges to RESIST/STOP — bare "smoke" — KEEP the quit-framed goal "not be a smoker"; smoking-scoped) + **contentless guard** (drop all-function-word fragments — "go back", "fix it though"; dev-task objects keep ≥1 content token → survive). Also crash-safe: removed the pydantic `max_length` cap (one over-long item raised ValidationError → crashed the whole entry) → soft length-drop in node; rate-aware fail-safe (429 propagates so live/backfill retry, other parse errors extract nothing). **Gate re-run N=3 = 0.962 mean, range 0.962, recall 1.0** over 66 fixtures (58 + 8 new), `evals/results/intention_extraction_2026-06-19T17-51-05*.json`. (Run on Vertex @6s pace: AI Studio key depleted, 0.8s trips Vertex QPM.)
- **P2 done** (`713735a`/`1dc8663`): resolution + persistence in `store_node` (`app/intention_resolver.py`) — re-reference vs new via embedding cosine. **`INTENTION_MATCH_THRESHOLD = 0.90`**, calibrated. **Bias to SPLIT**: a false MERGE silently destroys a real intention (folds into another row — invisible, unrecoverable); a false SPLIT is a dismissable duplicate. 0.90 = top of the 0.84–0.916 zero-false-merge plateau (max margin; 0.92+ sacrifices real restatements). `entry.created_at` (NOT now()) for `first_stated_at`/`last_referenced_at` so the **P3 backfill produces real historical drift**. Idempotent reprocess (source-entry check + 23505 swallow) + fail-safe per candidate. 9 hermetic tests; deadline/dedup suites unregressed (13/13). `test_store_node.py` red on pre-existing missing-pytest-asyncio infra (not drift).
- **P3 backfill — ✅ APPLIED 29/06 (43 live intentions, full personal seed).** User reviewed the dry-run → approved Groups A+B, cut 5 Group-C (extraction errors + mood/health non-intentions). Applied via new **`--exclude-text`** flag (commit `99f3ce0`, exclude-by-normalized-text, on `feat/drift-intentions` — **NOT merged**). Re-extract is nondeterministic: inserted **45**, rereferenced 1, skipped 0; the flag filtered 3 exact C-matches, **1 C-cut re-extracted as a variant ("look forward to drinking") and slipped past exact-match → soft-deleted post-apply** (reversible); the 5th didn't recur. **LIVE = 43** (45 inserted − the drinking variant − the degenerate single-word `mindgraph`, both soft-deleted post-apply), all 5 Group-C concepts confirmed absent (leakage scan clean). `first_stated_at`/`last_referenced_at` from `entry.created_at` — **drift verified historical+non-zero** (gym 46d/refs2, have money 87d, not-a-smoker 53d). Apply also surfaced a few new dev/life items not in the reviewed 48 + one degenerate single-word `mindgraph` — soft-deleted (44→43); the 3 new dev/life items KEPT as signal. Dev-vs-life categorize is P5.
  - **dev-TODO VOLUME** (many work intentions: rag/f1/mindgraph features) is a **P5 card-ranking/categorization** concern (life vs project), NOT an extraction miss — do NOT filter them out at extraction.
  - **Optional deferred one-liner (not grinding):** add time-adverbs (eventually/someday/soon/later) to `_NONCONTENT` — the lone curated FP ("sort that one … eventually") slips only because "eventually" reads as a content token.
- **P4 done** (`899bd64`): **`GET /intentions/drift`** — live `days_since(last_referenced_at)`, env-tunable `DRIFT_THRESHOLD_DAYS` (default 14, read **per-request** for live tuning; `?threshold_days=N` override). Returns ALL live intentions tagged `is_drifting`, sorted drift desc; **domain data only** (P5 maps to cards). New read service `app/services/intention_service.py` — separate from `intention_resolver` (write path). Fail-safe on unparseable timestamps. 7 hermetic tests (`tests/test_intention_drift.py`); verified vs the 43-seed (gym 46d, have money 87d, not-a-smoker 53d, **40/43 drifting @14**).
- **P5 done** (`6727229`): drift card — `buildDriftCards` maps `/intentions/drift` `is_drifting` items into the existing PatternsObservatory via a new calm `drift` PO_TYPE (muted blue-grey, **NO guilt/alarm** — witness-not-manager); dedicated drift group (PatternsObservatory parameterized: optional `eyebrow/noticed/sub/fallback`, defaults preserve existing use) + calm empty state; `id` carried for P6. Verified vs the 43-seed: **40 cards** sorted most-drifted-first (gym 47/refs2, have money 87, not-a-smoker 53), **0 guilt strings**, eslint clean. Life/project ranking deferred (not in P5 build scope; future polish).
- **P6 + P7 done** (`ec687ab`): lifecycle — `POST /intentions/{id}/resolve` (→ status `resolved`) + `/dismiss` (→ `dismissed`), **SOFT** + ownership-scoped (`WHERE id AND user_id AND deleted_at IS NULL` → 404 not-theirs/missing/soft-deleted, never deletes, idempotent). `get_drift`'s active/dormant whitelist excludes terminal statuses automatically (read/write **lockstep**). Frontend: card footer "Did this"/"Dismiss" (calm, witness-toned), optimistic remove + refetch + revert-on-failure. **No status CHECK constraint** (probed live) → no migration. 7 hermetic lifecycle tests + **P7 real-infra E2E**: resolve/dismiss one seed row each via the live route → vanishes from drift → restored; cross-user 404 (no mutation); count 40→39→40; **seed left untouched**. → **Drift P0–P7 critical path COMPLETE.**
- **NEXT (drift — launch, NOT another backend phase):** merge `feat/drift-intentions` → the real launch / demand-test. Deferred polish only: life/project card ranking; a dedicated `resolved_at`/`dismissed_at` column if analytics need finer than `updated_at`; time-diff matures post-launch.

## Next (ordered)
1. **Flip B on prod** — set Railway env `ASK_GENERATION_MODEL=gemini-3.1-flash-lite` + `ASK_GENERATION_THINKING=minimal` (gated on the 15/06 merge — satisfied). Rollback: delete both vars → reverts to `flash` (gemini-2.5-flash-lite, thinking_budget=0). Then run the first-prod-reask check (Watching).
2. Route "list my deadlines"-class Ask queries through structured `/deadlines` data instead of journal prose (P1 — `retrieve_relevant_entries` never queries the deadlines table; confirmed via code read 08/06).
3. Codebase Review 2026-06-05 — **remaining** critical finding (Notion page `3769402f…`): sequential-blocking "parallel" retrieval. (The two metering findings shipped — PR #5, live in prod.)
4. Edit + archive flows (entries/deadlines/projects from dashboard) — longstanding queued milestone.

## Known broken / degraded (open only)
- **Dedup-orphan empty entries — backfill pending (P2, 17/06, after the 0.92 merge):** 9 completed entries DB-wide have empty `cleaned_text`, ALL dedup-flagged (`dispatch_payload.pipeline_version='duplicate'`). Only **1 is a real-user FALSE positive** (entry 769bc801, ~51d gap to its alleged "match"); the other 8 are a test account / near-simultaneous true re-submits. Backfill sketch (do NOT run yet): for each empty+duplicate entry with non-empty `raw_text`, re-embed, re-run `match_entries`, and if sim ≤ 0.92 re-run the pipeline to populate it.
- **`Dashboard.test.js` suite red on main (P3, test-infra):** the deadlines mock URL (`?status=pending,snoozed`) predates the snapshot's `...,missed` variant → the snapshot never loads, projects don't render; suite catches nothing until fixed.
- **Dedup orphan rows — populate behavior still open (P2, next dedup item):** dashboard DISPLAY now excludes orphan rows (feed + counts, `fix/consolidate-staleness` Part C), but a TRUE duplicate STILL leaves an empty `completed` row in the DB that never advances `last_seen` — now hidden, not fixed. Fix direction: populate the row from the matched entry OR don't persist a dashboard-visible row.
- **Deferred metering follow-up (P2):** `POST /entries` + `/entries/stream` are unmetered (no rate limit, no cost cap) but have **no frontend caller** — direct-API surface only. Mirror `/entries/async`: add `entry_rate_limit` + `check_cost_cap`. **Trigger: close before any public/ungated API exposure.** (Carved out of the 15/06 metering PR.)
  - Cost cap meters on flat $0.0008 estimate, not measured cost; under-counts expensive calls; revisit with real per-call cost if abuse appears.
- **CI needs a programmatic re-ask judge with the fixed rubric** (P1): inline gemini-2.5-pro judging dropped 11/06 (quota troughs) and the session-agent batch judging used for the B experiment is non-reproducible. Candidate: flash-class judge + fixed rubric; harness `--no-judge` flag exists.
- Ask answers deadline-list queries from prose, not the deadlines table (P1).
- Read-after-write lag on `ask_messages` can defeat loop detection under rapid back-to-back sends (P2; not hit at human pace).
- Referenced-date indexing gap (Layer 3): entries mentioning a date aren't visible to temporal queries about that date — needs `entry_referenced_dates` table + pipeline extraction node (P2, ~4–6h).
- `ChatVertexAI` deprecation warning at every boot — migrate Vertex branch of `app/llm.py` to maintained class before LangChain 4.0 (P3).
- `normalize_thinking_budget_eval.py:97` calls `normalize()` with outdated `model=` kwarg — eval silently broken since ~23/05 (P3).
- **Stored past-event deadlines — cleanup pending (P2, 17/06, after the deadline-prompt merge):** 10 deadlines for the affected user have `due_date` < their source entry's `created_at` (logged already-past), 8 are clearly mis-extracted past activities (drank/hangover/gaming/woke-up), all `status=missed` and never closeable — these are the "past deadlines still open" the dashboard shows. **Delete UI merged** (PR #11): per-deadline ⋯ menu Delete (upcoming + missed). **Soft-delete + multi-delete + restore MERGED (PR #12) + LIVE in prod (`551938f`):** delete is now SOFT (`deleted_at`, migration **017 — `deleted_at` CONFIRMED present + the live deadline read path works (29/06, DB resumed; `_fetch_entry_deadlines` returned rows, no column error). REMAINING: confirm in the SQL editor that the 003 dedup unique index was recreated PARTIAL `WHERE deleted_at IS NULL` and that partial `idx_deadlines_live` exists — PostgREST exposes only `public`/`graphql_public` (PGRST106), so client introspection of `pg_indexes` isn't possible**; 017 recreates the 003 dedup unique index as partial `WHERE deleted_at IS NULL`, else reprocessing/restore breaks on duplicate-key). All **9 deadline read surfaces** filter `deleted_at IS NULL`: feed (`list_deadlines`), "N past deadlines still open" count (`_unmet_deadline_echo`), progress (`get_progress`), insights (`fetch_deadlines`), the 3 PATCH/delete guard SELECTs, entry-detail (`_fetch_entry_deadlines`), + the two `mark_overdue` reconcile UPDATEs. **`POST /deadlines/{id}/restore` live** (409 on live-duplicate). Rapid **multi-delete fixed** (single pending slot → per-id Map + stacked undo toasts; 2nd delete within 5s no longer dropped). Recovery = 5s undo + restore endpoint; **trash / "recently deleted" UI is a LOGGED FOLLOW-UP — not yet shipped.** Entry-cascade stays a hard delete (artifacts regenerable). Bulk cleanup sketch still pending (do NOT run yet): soft-delete/dismiss deadlines where `due_date < source_entry.created_at` AND description matches a completed-activity pattern (note: deadlines table has no `raw_text`/`created_at` column, so past-tense narration can't be re-checked from the row — use the source entry). Review the 2 borderline rows ("work on issue" 04-14, "wanted a card" 04-01) before acting.
- **Deadline HARD-zero (P3, residual — guard `drop_past_event_deadlines` merged + live `b557bf6` 18/06):** the guard took past-event leak 5/5→0/5 (N=5) but is not a HARD guarantee. **Scope:** keys on ENGLISH past-tense morphology (-ed / irregular pasts) — a precision filter for past-tense narration, NOT a universal past/future classifier. BLIND to (1) present-tense narration of past events ("go to the arcade"), (2) non-English/transliterated entries, (3) bare noun+date phrases. FAILS SAFE — when it can't classify, the deadline passes through; it never drops a real obligation. Universal coverage needs the model-based (thinking-budget) approach — **TRIGGER:** warranted when real present-tense or non-English traffic makes the residual leak rate climb. Do NOT extend the guard with more language rules — it is fundamentally English-morphology-bound.
- **Vertex quota — pre-launch check (P3, not now):** the eval 429s were an eval-burst artifact (≈60 rapid calls, ~half with thinking), NOT a prod risk at current traffic — one entry ≈ 6 `generate_content` calls (peak 4 concurrent at the `title_summary/classify/entities/deadline` fan-out), `thinking_budget=0`; a 429 → entry `status=error` (visible/retryable, not silent). **Pre-launch:** read the actual Vertex QPM/TPM for `gemini-2.5-flash-lite` in `us-central1` (GCP console → IAM & Admin → Quotas → Vertex AI API; unread during the investigation — Cloud Quotas API returned empty buckets). **Scale trigger:** a simultaneous-submission spike (4N concurrent calls; no concurrency cap, only `max_retries=2`) → add jittered backoff / an `asyncio.Semaphore` cap / a quota bump WHEN launch traffic warrants. Not now.
- Deadline live eval has stale April fixtures — F1 0.32/0.38 is a scoring artifact, not real signal (P3).
- Local `.env` has UTF-8 BOM breaking `os.getenv` locally; prod unaffected (P3).
- UI polish: desktop dashboard wasted space in wide layouts; deadline list scroll styling (P3).

## Watching (observation windows)
- **First-prod-reask check (after the B env flip, Next-1):** pull one real multi-turn re-ask from Langfuse and confirm turn-2 transforms (acknowledges + re-presents, not a redump). The synthetic grid predicts it (reask 0→4/5); prod confirms it on real retrieval. Also watch the Ask generation span p95 vs the >25s alert (harness p95 ~21.7s).
- **Vivek-class entity-gate advisory period — review date 09/06 has passed, decision overdue:** promote `question_entity_known` to active gating if logged false-positive rate < 1%; else tune the high-signal filter (shipped d01ee80).
- `want_all_not_subset` multi-turn eval category is judge-noise (inconsistent pass/fail on identical behavior) — review judge guidance before trusting its signal.
- `formal` persona produced no unique failures in the multi-turn grid — candidate for collapse to cut eval cost.
- **B's residual re-ask redumps** (the 2 reask fails in the audit subset — flat re-lists with an ack, e.g. frustrated/formal): known boundary of the model-swap fix, not a blocker (B 4/5 reask vs baseline 0/5). Candidate for a future fresh-branch prompt pass.
- **Context caching ($0.025/M)** — post-launch cost offset for the static Ask prompt prefix; revisit once B is live and traffic justifies it.
