# STATE — updated 2026-07-07 (🏠 Home capture-first restructure + drift pick v1 SHIPPED: nav = Home · Journal · Ask · Graph; Write+Today dissolved. NEXT: launch / demand-test.)

Maintained per ADR-0001: fixed/done items are **deleted** (history lives in the changelog DB + git), never struck through. Keep ≤1.5K tokens.

## Now (≤3)
1. **Vertex AI prod migration** (37fab70 + e2f8a4a; IAM 403 on `mindgraph-vertex` SA fixed 10/06, `roles/aiplatform.user`). Watch prod stability. (Conversation-route metering shipped — PR #5, live in prod `551938f`; post-merge audit GREEN.)
2. **3.1-flash-lite (B) MERGED 15/06 (PR #2) — only the prod env flip (Next-1) remains; flip vars confirmed in `app/llm.py` 29/06.** B (3.1-flash-lite, thinking_level=minimal, generation node only): reask transforms 13/15, judge mean 4.47 (0.03 under the 4.5 bar), overlap 0.286 vs A 0.487; C (low) fails (3.2). **External concordance audit** (gemini-3.1-pro-preview, 12 blind stratified cases, new seed) = **12/12 pass/fail agreement → external concordance confirmed (12/12)**. **clarifier_commit pre-check 15/15 PASS.** **Full 33-case run (Vertex) vs re-judged 85adc57 baseline, SAME fixed rubric: 32/33 vs 24/33 (+8); reask_loop 0→4/5, clarifier_commit 2→5/5, zero regressions, negatives hold → clears the ±4 noise band.** (Re-judging both neutralized the original Pro confounds: baseline reask 3/5 was trailing-Q luck → 0/5; want_all 2/5 was judge-noise → 5/5.) **Merged to main 15/06** (PR #2, merge `5f5d6da`; `cfaca4c`+`422df32` now on main). **Remaining: Railway env flip → watch prod** (Next-1). **Cost:** generation node $0.0008→$0.0018/conv measured (~2.25×); Batch API (−50%) for eval/offline; context caching ($0.025/M) a post-launch offset for the static prompt prefix. Results: `fullgrid_compare_B_vs_85adc57_*`, `concordance_audit_e7ef0b8e6ac3_*`, `clarifier_precheck_judged_*`, `blindjudge_reask_ABC_*`.

## Drift + Home capture-first IA — SHIPPED ✅ (drift 29/06; restructure + pick v1 + Journal v2 07/07)
LIVE hero feature. **IA: nav = Home · Journal · Ask · Graph** — Write+Today dissolved into **Home** (bounded-card composer; "Noticed" = 1 backend-picked drift card + reflection gift **capped at 3 insights** (`HOME_MAX_INSIGHTS`, hotfix PR #14 after the opened-gift flood); Recent 3; default route, legacy `#write`/`#dashboard` fall through to it) and **Journal v2 = ONE scrollable life view, no sub-tabs** (PR #15): On your plate (next 3 deadlines + active projects, full row actions; overflow expander carries extra/missed/hidden) → Patterns (FULL reflection set) → Intentions (raw rows: text · days quiet · status · actions + bulk dismiss w/ per-id map; **PIN: drift framing = po-card/Drifting pill, Home-only; "days quiet" is data, not framing**) → Entries (reverse-chron, **infinite scroll only** — the page-numbers+load-more double-render bug is gone; filters behind one collapsed control). Red-urgency deadline treatment removed; sidebar showed-up counter, masthead/tagline/ticker deleted (approved 07/07). **Drift pick v1** (migration 020 `surfaced_at`, applied): `GET /intentions/drift?pick=true` — pending ∧ threshold≤days≤90 ∧ (never surfaced ∨ >14d cooldown); score `2·log2(1+refs) + maturity_band(peak 7–35d) + 0.5 never-surfaced`. **SPEC DECISION: stickiness = 48h-unacted window (not same-calendar-day); sticky re-serves don't restamp/re-log; acting rotates immediately.** **Self-judgment guard (HARD, pick-time only):** "Not be a useless guy" / "Have an identity" (real fixtures, test-pinned) can never be the Home card; Journal still lists them. **Backend PostHog:** `drift_card_served` (once per pick: id/score/days_since/refs), `intention_resolved`/`intention_dismissed`. Prod-verified end-to-end incl. rendered smokes (`scripts/smoke_home_prod.js`, `smoke_journal_prod.js`, `verify_drift_pick_prod.py` — sticky-aware).
- **NOW → LAUNCH / demand-test (NOT another backend phase):** put it in front of strangers — 3+ entries each — and watch for the "that's the gap" reaction.
- **Deferred (post-launch — do NOT pre-build):** life/project card ranking + dev-vs-life categorization; a dedicated `resolved_at`/`dismissed_at` column if analytics need finer than `updated_at`; time-diff maturation; the optional time-adverb (`eventually/someday/soon`) addition to `_NONCONTENT`; full design in docs/designs/intention-clustering.md (clustering at extraction + intention_projects join; trigger: real user >15 pending intentions); pick-score weights re-tuned from `drift_card_served` telemetry once real serves accumulate.

## Reflection (self-synthesis) — SHIPPED ✅ (deployed to prod `d3903e3`, 02/07)
Evolving per-user self-understanding doc surfacing NON-OBVIOUS behavioural patterns from FULL journal text — replaces the retired shallow "pattern" cards (mention-count bookkeeping). Distinct from drift (drift = stated-intent-vs-behaviour; this = who you are across your writing). Engine `app/synthesis_engine.py` + migration 019 `user_synthesis` (applied to prod): read existing doc + full `raw_text` since watermark → gemini-2.5-pro rewrite → upsert; bounded-doc "keep strongest ≤7, no dupes" (clone of `build_compaction_prompt`). Cadence: debounced fire-and-forget after each entry (gated: stale ≥ `REFLECTION_STALE_DAYS`=3 AND new entries; first gift ≥ `REFLECTION_MIN_ENTRIES`=5) — no scheduler. UX: `GET`/`POST /insights/synthesis[/open]`; per-insight "gift" cards with a lid-flip open animation; re-wraps (`opened_at`=null) on regen; **gift renders in Home's "Noticed" (capped at 3 leading insights) and in Journal → Patterns (full set), wrapped if unopened / revealed if opened.** Deterministic input eval GREEN 11/11 (`evals/eval_reflection_synthesis.py`). `generate_patterns` RETIRED (not called; `/insights/patterns` route dormant). `forgotten_projects` still generated + `/insights` route live but UNCONSUMED (reversible — restore render from git).
- **NOW → launch / demand-test** (same as drift). Only krithikb4u is seeded so far; other users generate a first gift once they cross 5 entries.
- **Deferred (do NOT pre-build):** per-card open PERSISTENCE (currently first-open marks the whole gift seen → reload shows all revealed); a scheduler for inactive-user regen; tune stale/min-entries once real traffic shows cadence. (The opened-reflections-placement trigger fired same-day and is RESOLVED: Home capped at 3, full set in Journal → Patterns.)

## Next (ordered)
1. **Flip B on prod** — set Railway env `ASK_GENERATION_MODEL=gemini-3.1-flash-lite` + `ASK_GENERATION_THINKING=minimal` (gated on the 15/06 merge — satisfied). Rollback: delete both vars → reverts to `flash` (gemini-2.5-flash-lite, thinking_budget=0). Then run the first-prod-reask check (Watching).
2. Codebase Review 2026-06-05 — **remaining** critical finding (Notion page `3769402f…`): sequential-blocking "parallel" retrieval. (The two metering findings shipped — PR #5, live in prod.)
3. Edit + archive flows (entries/deadlines/projects from Journal) — longstanding queued milestone.

## Known broken / degraded (open only)
- **AI Studio prepay depleted — local LLM path dead (P3):** the AI Studio `GEMINI_API_KEY` returns 429 RESOURCE_EXHAUSTED. **Prod is unaffected** (Vertex, `USE_VERTEX=1`, GCP-billed). Any LOCAL Pro/flash run must force the Vertex path — local gcloud ADC works (project `psychic-surf-498910-f7`, `us-central1`). Top up the AI Studio key or just use Vertex locally.
- **Dedup-orphan empty entries — backfill pending (P2, 17/06, after the 0.92 merge):** 9 completed entries DB-wide have empty `cleaned_text`, ALL dedup-flagged (`dispatch_payload.pipeline_version='duplicate'`). Only **1 is a real-user FALSE positive** (entry 769bc801, ~51d gap to its alleged "match"); the other 8 are a test account / near-simultaneous true re-submits. Backfill sketch (do NOT run yet): for each empty+duplicate entry with non-empty `raw_text`, re-embed, re-run `match_entries`, and if sim ≤ 0.92 re-run the pipeline to populate it.
- **MyProgress orphaned + two red suites riding along (P2 — DECIDE NEXT SESSION):** MyProgress is a hash-only `#/progress` view, never in nav; decide DELETE vs keep, its suite goes with it. `MyProgress.test.js` (6F) and `AskView.test.js` (3F) have now ridden through THREE PRs (#13/#14/#15) as "pre-existing red" — fix or delete both next session so the suite baseline is 0F.
- **Dormant backend surfaces (P3, deletion candidates):** `/stats/showed-up` (frontend consumer — the sidebar counter — removed 07/07); weekly weather-tagline generation (`/insights/tagline`, consumed by nothing since Today retired); `forgotten_projects` insight (unconsumed since `b1c5be3`). All reversible; delete or leave until launch settles.
- **Dedup orphan rows — populate behavior still open (P2, next dedup item):** dashboard DISPLAY now excludes orphan rows (feed + counts, `fix/consolidate-staleness` Part C), but a TRUE duplicate STILL leaves an empty `completed` row in the DB that never advances `last_seen` — now hidden, not fixed. Fix direction: populate the row from the matched entry OR don't persist a dashboard-visible row.
- **Cost-cap cost model — flat estimate fallback (P2):** `record_cost` uses the actual Langfuse trace cost when available, else a fixed per-type estimate (`entry`=$0.003, `ask`=$0.0008; `app/services/cost_cap.py`); the estimate can under-count expensive calls. Revisit with real per-call cost if abuse appears. (The unmetered-`/entries`+`/entries/stream` follow-up is CLOSED — both now metered, `a9de0ac`, mirror `/entries/async`.)
- **CI needs a programmatic re-ask judge with the fixed rubric** (P1): inline gemini-2.5-pro judging dropped 11/06 (quota troughs) and the session-agent batch judging used for the B experiment is non-reproducible. Candidate: flash-class judge + fixed rubric; harness `--no-judge` flag exists.
- **Stale deadline Ask eval case — re-baseline pending (P3, follow-up to the 30/06 deadline-retrieval fix):** `evals/rag_test_cases.json` case "What are my deadlines for the end of May?" is `category:"temporal"` and scored on PROSE-entry retrieval F1 (`expected_entry_id` + keywords) — it encodes the old "answer deadlines from journal prose" behavior as correct, so it no longer reflects the fixed path (deadlines now come from the structured table via `dashboard_context`). Decide: re-categorize/retire it, or convert to an answer-content assertion like the new `evals/eval_ask_deadlines.py`. Not urgent — `eval_ask_retrieval.py` still scores it on retrieval, not answer quality.
- Read-after-write lag on `ask_messages` can defeat loop detection under rapid back-to-back sends (P2; not hit at human pace).
- Referenced-date indexing gap (Layer 3): entries mentioning a date aren't visible to temporal queries about that date — needs `entry_referenced_dates` table + pipeline extraction node (P2, ~4–6h).
- `ChatVertexAI` deprecation warning at every boot — migrate Vertex branch of `app/llm.py` to maintained class before LangChain 4.0 (P3).
- `normalize_thinking_budget_eval.py:97` calls `normalize()` with outdated `model=` kwarg — eval silently broken since ~23/05 (P3).
- **Stored past-event deadlines — cleanup pending (P2, 17/06, after the deadline-prompt merge):** 10 deadlines for the affected user have `due_date` < their source entry's `created_at` (logged already-past), 8 are clearly mis-extracted past activities (drank/hangover/gaming/woke-up), all `status=missed` and never closeable — these are the "past deadlines still open" the dashboard shows. **Delete UI merged** (PR #11): per-deadline ⋯ menu Delete (upcoming + missed). **Soft-delete + multi-delete + restore MERGED (PR #12) + LIVE in prod (`551938f`):** delete is now SOFT (`deleted_at`, migration **017 — CONFIRMED applied (29/06, SQL editor): `deleted_at` present, the live deadline read path works (`_fetch_entry_deadlines` returns rows), AND both `idx_deadlines_source_entry_description_due_date` (UNIQUE) and `idx_deadlines_live` carry the partial `WHERE deleted_at IS NULL` predicate**; 017 recreates the 003 dedup unique index as partial `WHERE deleted_at IS NULL`, else reprocessing/restore breaks on duplicate-key). All **9 deadline read surfaces** filter `deleted_at IS NULL`: feed (`list_deadlines`), "N past deadlines still open" count (`_unmet_deadline_echo`), progress (`get_progress`), insights (`fetch_deadlines`), the 3 PATCH/delete guard SELECTs, entry-detail (`_fetch_entry_deadlines`), + the two `mark_overdue` reconcile UPDATEs. **`POST /deadlines/{id}/restore` live** (409 on live-duplicate). Rapid **multi-delete fixed** (single pending slot → per-id Map + stacked undo toasts; 2nd delete within 5s no longer dropped). Recovery = 5s undo + restore endpoint; **trash / "recently deleted" UI is a LOGGED FOLLOW-UP — not yet shipped.** Entry-cascade stays a hard delete (artifacts regenerable). Bulk cleanup sketch still pending (do NOT run yet): soft-delete/dismiss deadlines where `due_date < source_entry.created_at` AND description matches a completed-activity pattern (note: deadlines table has no `raw_text`/`created_at` column, so past-tense narration can't be re-checked from the row — use the source entry). Review the 2 borderline rows ("work on issue" 04-14, "wanted a card" 04-01) before acting.
- **Deadline HARD-zero (P3, residual — guard `drop_past_event_deadlines` merged + live `b557bf6` 18/06):** the guard took past-event leak 5/5→0/5 (N=5) but is not a HARD guarantee. **Scope:** keys on ENGLISH past-tense morphology (-ed / irregular pasts) — a precision filter for past-tense narration, NOT a universal past/future classifier. BLIND to (1) present-tense narration of past events ("go to the arcade"), (2) non-English/transliterated entries, (3) bare noun+date phrases. FAILS SAFE — when it can't classify, the deadline passes through; it never drops a real obligation. Universal coverage needs the model-based (thinking-budget) approach — **TRIGGER:** warranted when real present-tense or non-English traffic makes the residual leak rate climb. Do NOT extend the guard with more language rules — it is fundamentally English-morphology-bound.
- **Vertex quota — pre-launch check (P3, not now):** the eval 429s were an eval-burst artifact (≈60 rapid calls, ~half with thinking), NOT a prod risk at current traffic — one entry ≈ 6 `generate_content` calls (peak 4 concurrent at the `title_summary/classify/entities/deadline` fan-out), `thinking_budget=0`; a 429 → entry `status=error` (visible/retryable, not silent). **Pre-launch:** read the actual Vertex QPM/TPM for `gemini-2.5-flash-lite` in `us-central1` (GCP console → IAM & Admin → Quotas → Vertex AI API; unread during the investigation — Cloud Quotas API returned empty buckets). **Scale trigger:** a simultaneous-submission spike (4N concurrent calls; no concurrency cap, only `max_retries=2`) → add jittered backoff / an `asyncio.Semaphore` cap / a quota bump WHEN launch traffic warrants. Not now.
- Deadline live eval has stale April fixtures — F1 0.32/0.38 is a scoring artifact, not real signal (P3).
- Local `.env` has UTF-8 BOM breaking `os.getenv` locally; prod unaffected (P3).
- UI polish: desktop dashboard wasted space in wide layouts; deadline list scroll styling (P3).

## Watching (observation windows)
- **Reflection cadence in prod:** confirm the debounced post-entry regen actually fires + writes a first gift once a real user crosses 5 entries (only krithikb4u seeded so far). `opened_at` was manually reset for review — a real regen resets it automatically.
- **First-prod-reask check (after the B env flip, Next-1):** pull one real multi-turn re-ask from Langfuse and confirm turn-2 transforms (acknowledges + re-presents, not a redump). The synthetic grid predicts it (reask 0→4/5); prod confirms it on real retrieval. Also watch the Ask generation span p95 vs the >25s alert (harness p95 ~21.7s).
- **Vivek-class entity-gate advisory period — review date 09/06 has passed, decision overdue:** promote `question_entity_known` to active gating if logged false-positive rate < 1%; else tune the high-signal filter (shipped d01ee80).
- `want_all_not_subset` multi-turn eval category is judge-noise (inconsistent pass/fail on identical behavior) — review judge guidance before trusting its signal.
- `formal` persona produced no unique failures in the multi-turn grid — candidate for collapse to cut eval cost.
- **B's residual re-ask redumps** (the 2 reask fails in the audit subset — flat re-lists with an ack, e.g. frustrated/formal): known boundary of the model-swap fix, not a blocker (B 4/5 reask vs baseline 0/5). Candidate for a future fresh-branch prompt pass.
- **Context caching ($0.025/M)** — post-launch cost offset for the static Ask prompt prefix; revisit once B is live and traffic justifies it.
